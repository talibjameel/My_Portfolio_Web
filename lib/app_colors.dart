import 'package:flutter/material.dart';

class AppColors {
  /// 1. Background Color
  static const Color background1 = Color(0xFF2D2E32);

 /// 2. Background Color
  static const Color background2 = Color(0xFF25262A);

  /// 2. Green Color (Buttons, h2 heading, success tone)
  static const Color green = Color(0xFF6EF3A5);


  /// 3. Grey Color (Visual hierarchy support)
  static const Color grey = Color(0xFFA0A0A0);

  /// 4. White Color (Text on dark backgrounds, tooltips)
  static const Color white = Color(0xFFFFFFFF);
}
